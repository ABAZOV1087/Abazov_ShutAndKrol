<Page x:Class="Abazov_ShutAndKrol.Pages.AuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Abazov_ShutAndKrol.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Панель автора" Loaded="Page_Loaded">

    <Grid Background="#FFFFFF" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,15">
            <TextBlock Text="Мои книги" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="btnAddBook" Content="+ Добавить книгу" Margin="20,0,0,0" Padding="15,7" Click="btnAddBook_Click" Background="#2ECC71" Foreground="White" BorderBrush="Transparent" FontWeight="Bold"/>
        </StackPanel>

        <DataGrid x:Name="dgAuthorBooks" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" MouseDoubleClick="dgAuthorBooks_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="3*"/>
                <DataGridCheckBoxColumn Header="Заморожена" Binding="{Binding IsFrozen}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>