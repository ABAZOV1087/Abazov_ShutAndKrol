<Page x:Class="Abazov_ShutAndKrol.Pages.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Abazov_ShutAndKrol.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
Title="Регистрация">

    <Grid Background="#F5F7FA">
        <Border Background="#FFFFFF" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.1" ShadowDepth="3"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Регистрация" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <TextBlock Text="Логин *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                <TextBox x:Name="tbLogin" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                <TextBox x:Name="tbPassword" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,15"/>

                <TextBlock Text="Email *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                <TextBox x:Name="tbEmail" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,15"/>

                <TextBlock Text="Отображаемое имя (необязательно)" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                <TextBox x:Name="tbDisplayName" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,25"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnBack" Grid.Column="0" Content="Назад" Height="40" Background="#95A5A6" Foreground="White" BorderBrush="Transparent" Click="btnBack_Click" Margin="0,0,5,0"/>
                    <Button x:Name="btnRegister" Grid.Column="1" Content="Создать аккаунт" Height="40" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderBrush="Transparent" Click="btnRegister_Click" Margin="5,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
